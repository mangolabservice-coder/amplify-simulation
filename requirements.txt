# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
pydantic>=2.11.5,<3
pydantic-settings>=2.5.2

# RAG / LlamaIndex (core only — avoid meta `llama-index` pulling its own OpenAI stack)
llama-index-core==0.14.21
llama-index-vector-stores-qdrant==0.10.0
llama-index-embeddings-huggingface==0.7.0
llama-index-readers-file==0.6.0

# Vector store (>=1.16 required by llama-index-vector-stores-qdrant 0.10.x)
qdrant-client>=1.16.0,<2

# LLM clients (used by app/llm/*, not via LlamaIndex)
openai>=1.51.0,<2
httpx>=0.27.2,<1

# Utilities
python-dotenv==1.0.1
pypdf>=6.1.3,<7
