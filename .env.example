# Optional overrides for this app only. Later files override earlier ones; see app/config.py.
# OPENAI_API_KEY is also read from ../../.env and ../../fritz-hub/.env (same key as n8n OpenAI in Fritz Hub).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
QDRANT_URL=http://localhost:6333
